# --- Node / Package Managers ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json.lock
.pnpm-store/
.npm/
.cache/
paste.txt
*.worker.js
dist/
build/
.next/

# --- Next.js / Build Artifacts ---
.next/
out/
.next-turbo/
.vercel/
.vite/
.vite-ssr/
*.tsbuildinfo

# Keep Next.js type shim (commit this)
!next-env.d.ts

# --- Testing / Reports / Temp ---
coverage/
playwright-report/
test-results/
tests/playwright/.sessions/
*.lcov
*.snap
.tmp/
tmp/
*.pid
*.seed
*.log
*.log.*

# --- E2E Artifacts (screenshots/videos if configured) ---
**/__screenshots__/
**/__image_snapshots__/
**/__diff_output__/
**/videos/

# --- OS / Editor Junk ---
.DS_Store
Thumbs.db
Icon?
ehthumbs.db
*.swp
*.swo
*.orig
.idea/
.vscode/
.history/
*.iml

# --- Env / Secrets (keep a template committed as .env.example) ---
.env
.env.*
!.env.example
!.env.sample

# --- Azure / Cloud local state ---
.azure/
.azuredevops/
*.pubxml
*.Publish.xml

# --- Terraform / IaC (if used later) ---
.terraform/
terraform.tfstate
terraform.tfstate.*
crash.log
*.tfvars
*.tfvars.json

# --- Prisma / Drizzle (if added later) ---
prisma/*.db
prisma/migrations/.cache/
drizzle/*.sqlite
drizzle/*.db
drizzle/migrations/.cache/

# --- Upload / Cache Buckets (runtime) ---
uploads/
storage/
.cache-loader/
.cache-data/

# --- App-specific temporary outputs ---
public/**/*.tmp
public/**/*.bak

# --- Monorepo / Tooling noise (safe if not used) ---
dist/
build/
coverage-final.json
lerna-debug.log
*.tgz

# --- Playwright browsers cache (when using install deps) ---
ms-playwright/

# --- VSCode test coverage, Jest cache ---
.jest/
jest-cache/
jest-results.json

# --- Misc generated by scripts ---
scripts/**/*.log
scripts/**/out/**
scripts/**/dist/**

# --- Azure key rotation scripts (contain secret patterns) ---
scripts/rotate-azure-keys.ps1
scripts/rotate-keys-NOW.ps1
scripts/smoke-test.ps1

# --- Git merge artifacts ---
*.rej
*.patch
*.msi
.vercel

# --- Sample/Dummy PDF files in root ---
/*.pdf
